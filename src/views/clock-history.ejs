<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lịch sử bộ đếm | Cố Tinh Đạo Tông</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <script src="/js/theme-init.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=EB+Garamond:wght@400;500;600;700&family=Crimson+Text:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .history-container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 0 20px;
        }

        .history-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .history-header h1 {
            color: var(--danger-color);
            font-size: 2rem;
            margin: 0;
        }

        .back-btn {
            padding: 0.5rem 1rem;
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-end) 100%);
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 600;
            transition: transform 0.3s;
        }

        .back-btn:hover {
            transform: translateY(-2px);
        }

        .history-card {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 4px 15px var(--shadow-color);
            border: 1px solid var(--border-color);
        }

        .history-card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
            flex-wrap: wrap;
            gap: 0.5rem;
        }

        .streak-duration {
            font-size: 1.75rem;
            font-weight: bold;
            color: var(--danger-color);
        }

        .streak-dates {
            color: var(--text-secondary);
            font-size: 0.85rem;
        }

        .history-card-body {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .date-info {
            display: flex;
            gap: 2rem;
            flex-wrap: wrap;
        }

        .date-item {
            text-align: center;
        }

        .date-item label {
            display: block;
            color: var(--text-secondary);
            font-size: 0.75rem;
            text-transform: uppercase;
            margin-bottom: 0.25rem;
        }

        .date-item span {
            color: var(--text-primary);
            font-weight: 500;
        }

        .delete-btn {
            padding: 0.5rem 1rem;
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.85rem;
            transition: background 0.3s;
        }

        .delete-btn:hover {
            background: #c0392b;
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: var(--text-secondary);
            background: var(--card-bg);
            border-radius: 12px;
            box-shadow: 0 2px 10px var(--shadow-color);
        }

        .empty-state p {
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .delete-all-section {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border-color);
            text-align: center;
        }

        .delete-all-btn {
            padding: 0.75rem 1.5rem;
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            transition: background 0.3s;
        }

        .delete-all-btn:hover {
            background: #c0392b;
        }

        .summary-box {
            background: linear-gradient(135deg, #0f3460 0%, #1a1a2e 100%);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .summary-item {
            text-align: center;
        }

        .summary-item .value {
            font-size: 2rem;
            font-weight: bold;
            color: #e94560;
        }

        .summary-item .label {
            color: #888;
            font-size: 0.8rem;
            text-transform: uppercase;
        }

        @media (max-width: 600px) {
            .history-header {
                flex-direction: column;
                align-items: flex-start;
            }

            .history-header h1 {
                font-size: 1.5rem;
            }

            .streak-duration {
                font-size: 1.25rem;
            }

            .date-info {
                gap: 1rem;
            }

            .summary-box {
                flex-direction: column;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="history-container">
            <div class="history-header">
                <h1>🧠 Lịch sử bộ đếm ngày tu luyện</h1>
                <a href="/dashboard" class="back-btn">← Về tổng quan</a>
            </div>

            <% if (clockHistory && clockHistory.length> 0) { %>
                <!-- Summary -->
                <div class="summary-box">
                    <div class="summary-item">
                        <div class="value">
                            <%= clockHistory.length %>
                        </div>
                        <div class="label">Tổng số lần đặt lại</div>
                    </div>
                    <div class="summary-item">
                        <% const longestStreak=Math.max(...clockHistory.map(h=> h.duration_seconds));
                            const longestDays = Math.floor(longestStreak / 86400);
                            const longestHours = Math.floor((longestStreak % 86400) / 3600);
                            %>
                            <div class="value">
                                <%= longestDays %>d <%= longestHours %>h
                            </div>
                            <div class="label">Chuỗi dài nhất</div>
                    </div>
                    <div class="summary-item">
                        <% const avgSeconds=Math.floor(clockHistory.reduce((sum, h)=> sum + h.duration_seconds, 0) /
                            clockHistory.length);
                            const avgDays = Math.floor(avgSeconds / 86400);
                            const avgHours = Math.floor((avgSeconds % 86400) / 3600);
                            %>
                            <div class="value">
                                <%= avgDays %>d <%= avgHours %>h
                            </div>
                            <div class="label">Trung bình chuỗi</div>
                    </div>
                </div>

                <!-- History List -->
                <% clockHistory.forEach((record, index)=> {
                    const days = Math.floor(record.duration_seconds / 86400);
                    const hours = Math.floor((record.duration_seconds % 86400) / 3600);
                    const minutes = Math.floor((record.duration_seconds % 3600) / 60);
                    const seconds = record.duration_seconds % 60;

                    const startDate = new Date(record.start_time);
                    const endDate = new Date(record.end_time);
                    const formatDate = (d) => d.toLocaleDateString('vi-VN', { month: 'short', day: 'numeric', year:
                    'numeric' });
                    const formatTime = (d) => d.toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit' });
                    %>
                    <div class="history-card">
                        <div class="history-card-header">
                            <div>
                                <div class="streak-duration">
                                    🔥 <%= days %> ngày, <%= hours %> giờ <%= minutes %> phút <%= seconds %> giây
                                </div>
                                <div class="streak-dates">
                                    <%= formatDate(startDate) %> → <%= formatDate(endDate) %>
                                </div>
                            </div>
                            <span style="color: var(--info-color); font-size: 0.9rem;">#<%= clockHistory.length - index
                                    %></span>
                        </div>
                        <div class="history-card-body">
                            <div class="date-info">
                                <div class="date-item">
                                    <label>Bắt đầu</label>
                                    <span>
                                        <%= formatDate(startDate) %>
                                            <%= formatTime(startDate) %>
                                    </span>
                                </div>
                                <div class="date-item">
                                    <label>Đặt lại</label>
                                    <span>
                                        <%= formatDate(endDate) %>
                                            <%= formatTime(endDate) %>
                                    </span>
                                </div>
                            </div>
                            <form method="POST" action="/clock-history/<%= record.id %>/delete" style="margin: 0;">
                                <button type="submit" class="delete-btn"
                                    onclick="return confirm('Xoá bản ghi lịch sử bộ đếm này?');">
                                    🗑️ Xoá
                                </button>
                            </form>
                        </div>
                    </div>
                    <% }); %>

                        <div class="delete-all-section">
                            <form method="POST" action="/clock-history/delete-all">
                                <button type="submit" class="delete-all-btn"
                                    onclick="return confirm('Bạn chắc chắn muốn xoá TOÀN BỘ lịch sử bộ đếm? Hành động này không thể hoàn tác.');">
                                    🗑️ Xoá toàn bộ lịch sử bộ đếm
                                </button>
                            </form>
                        </div>
                        <% } else { %>
                            <div class="empty-state">
                                <p>📭 Chưa có lịch sử bộ đếm</p>
                                <p style="font-size: 0.9rem;">Hãy bắt đầu bộ đếm ngày tu luyện và đặt lại để xem lịch sử
                                    hiển thị tại đây.</p>
                                <a href="/dashboard" class="back-btn" style="display: inline-block; margin-top: 1rem;">←
                                    Về tổng quan</a>
                            </div>
                            <% } %>
        </div>

        <%- include('partials/footer') %>
</body>

</html>