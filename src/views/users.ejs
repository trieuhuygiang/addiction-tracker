<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Người dùng | Tông Môn Tu Tiên</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <script src="/js/theme-init.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=EB+Garamond:wght@400;500;600;700&family=Crimson+Text:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .users-container {
            max-width: 1000px;
            margin: 2rem auto;
            padding: 2rem;
            background: var(--card-bg);
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
        }

        .users-container h1 {
            color: var(--info-color);
            margin-bottom: 2rem;
            border-bottom: 2px solid var(--info-color);
            padding-bottom: 1rem;
        }

        .users-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 2rem;
        }

        .users-table thead {
            background: var(--bg-tertiary);
        }

        .users-table th,
        .users-table td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
            color: var(--text-primary);
        }

        .users-table th {
            font-weight: 600;
            color: var(--text-primary);
        }

        .users-table tbody tr:hover {
            background: var(--bg-tertiary);
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.9rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s;
        }

        .btn-edit {
            background: var(--info-color);
            color: white;
        }

        .btn-edit:hover {
            background: color-mix(in srgb, var(--info-color) 82%, black);
        }

        .btn-delete {
            background: var(--danger-color);
            color: white;
        }

        .btn-delete:hover {
            background: color-mix(in srgb, var(--danger-color) 82%, black);
        }

        .btn-primary {
            background: var(--success-color);
            color: white;
            margin-bottom: 1.5rem;
        }

        .btn-primary:hover {
            background: color-mix(in srgb, var(--success-color) 82%, black);
        }

        .create-user-form {
            background: var(--bg-tertiary);
            padding: 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--text-primary);
            font-weight: 600;
        }

        .form-group input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            font-size: 1rem;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--info-color);
            box-shadow: 0 0 0 3px color-mix(in srgb, var(--info-color) 18%, transparent);
        }

        .error {
            background: var(--card-bg);
            color: var(--danger-color);
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
            border: 1px solid color-mix(in srgb, var(--danger-color) 28%, transparent);
        }

        .success {
            background: var(--card-bg);
            color: var(--success-color);
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
            border: 1px solid color-mix(in srgb, var(--success-color) 28%, transparent);
        }

        .no-users {
            text-align: center;
            padding: 2rem;
            color: var(--text-secondary);
            font-size: 1.1rem;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
        }

        .modal.show {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .modal-content {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: 10px;
            max-width: 500px;
            width: 90%;
            box-shadow: 0 4px 20px var(--shadow-color);
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--info-color);
        }

        .modal-header h2 {
            margin: 0;
            color: var(--info-color);
        }

        .close-btn {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-secondary);
        }

        .close-btn:hover {
            color: var(--text-primary);
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="users-container">
            <h1>Quản lý người dùng</h1>

            <% if (success) { %>
                <div class="success">
                    <%= success %>
                </div>
                <% } %>

                    <% if (error) { %>
                        <div class="error">
                            <%= error %>
                        </div>
                        <% } %>

                            <button class="btn btn-primary" onclick="openCreateModal()">+ Tạo người dùng mới</button>

                            <% if (users && users.length> 0) { %>
                                <table class="users-table">
                                    <thead>
                                        <tr>
                                            <th>Email</th>
                                            <th>Tên đăng nhập</th>
                                            <th>Ngày tạo</th>
                                            <th>Thao tác</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% users.forEach(user=> { %>
                                            <tr>
                                                <td>
                                                    <%= user.email %>
                                                </td>
                                                <td>
                                                    <%= user.username || '-' %>
                                                </td>
                                                <td>
                                                    <%= new Date(user.created_at).toLocaleDateString('vi-VN') %>
                                                </td>
                                                <td>
                                                    <div class="action-buttons">
                                                        <button class="btn btn-edit"
                                                            onclick="openEditModal(<%= user.id %>, '<%= user.email %>', '<%= user.username || '' %>')">Sửa</button>
                                                        <form method="POST" action="/users/<%= user.id %>/delete"
                                                            style="display: inline;"
                                                            onsubmit="return confirm('Bạn chắc chắn muốn xoá?');">
                                                            <button type="submit" class="btn btn-delete">Xoá</button>
                                                        </form>
                                                    </div>
                                                </td>
                                            </tr>
                                            <% }); %>
                                    </tbody>
                                </table>
                                <% } else { %>
                                    <div class="no-users">
                                        Chưa có người dùng. Hãy tạo mới để bắt đầu!
                                    </div>
                                    <% } %>
        </div>

        <!-- Create User Modal -->
        <div id="createModal" class="modal">
            <div class="modal-content">
                <div class="modal-header">
                    <h2>Tạo người dùng mới</h2>
                    <button class="close-btn" onclick="closeCreateModal()">&times;</button>
                </div>
                <form method="POST" action="/users/create">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="username">Tên đăng nhập (tuỳ chọn)</label>
                        <input type="text" id="username" name="username">
                    </div>
                    <div class="form-group">
                        <label for="password">Mật khẩu</label>
                        <input type="password" id="password" name="password" required minlength="6">
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Xác nhận mật khẩu</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" required minlength="6">
                    </div>
                    <div class="form-row">
                        <button type="button" class="btn" onclick="closeCreateModal()"
                            style="background: var(--text-secondary); color: white;">Huỷ</button>
                        <button type="submit" class="btn btn-primary">Tạo người dùng</button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Edit User Modal -->
        <div id="editModal" class="modal">
            <div class="modal-content">
                <div class="modal-header">
                    <h2>Sửa người dùng</h2>
                    <button class="close-btn" onclick="closeEditModal()">&times;</button>
                </div>
                <form method="POST" action="/users/update" id="editForm">
                    <input type="hidden" id="editUserId" name="userId">
                    <div class="form-group">
                        <label for="editEmail">Email</label>
                        <input type="email" id="editEmail" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="editUsername">Tên đăng nhập (tuỳ chọn)</label>
                        <input type="text" id="editUsername" name="username">
                    </div>
                    <div class="form-group">
                        <label for="editPassword">Mật khẩu mới (để trống nếu giữ nguyên)</label>
                        <input type="password" id="editPassword" name="password" minlength="6">
                    </div>
                    <div class="form-row">
                        <button type="button" class="btn" onclick="closeEditModal()"
                            style="background: var(--text-secondary); color: white;">Huỷ</button>
                        <button type="submit" class="btn btn-primary">Cập nhật</button>
                    </div>
                </form>
            </div>
        </div>

        <script>
            function openCreateModal() {
                document.getElementById('createModal').classList.add('show');
            }

            function closeCreateModal() {
                document.getElementById('createModal').classList.remove('show');
            }

            function openEditModal(userId, email, username) {
                document.getElementById('editUserId').value = userId;
                document.getElementById('editEmail').value = email;
                document.getElementById('editUsername').value = username;
                document.getElementById('editModal').classList.add('show');
            }

            function closeEditModal() {
                document.getElementById('editModal').classList.remove('show');
            }

            // Close modal when clicking outside
            window.onclick = function (event) {
                const createModal = document.getElementById('createModal');
                const editModal = document.getElementById('editModal');
                if (event.target == createModal) {
                    closeCreateModal();
                }
                if (event.target == editModal) {
                    closeEditModal();
                }
            }
        </script>

        <%- include('partials/footer') %>
</body>

</html>