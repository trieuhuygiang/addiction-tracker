<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> - Addiction Tracker
    </title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .calendar-year-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        .calendar-year-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .calendar-year-header h1 {
            color: #667eea;
            margin-bottom: 0.5rem;
            font-size: 2rem;
        }

        .calendar-year-header p {
            color: var(--text-secondary);
            font-size: 1rem;
        }

        .year-navigation {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .year-nav-btn {
            padding: 0.5rem 1rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.3s;
        }

        .year-nav-btn:hover {
            transform: translateY(-2px);
        }

        .year-display {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--text-primary);
            min-width: 200px;
            text-align: center;
        }

        .view-toggle {
            display: flex;
            justify-content: center;
            gap: 0.5rem;
            margin-bottom: 2rem;
        }

        .view-toggle a {
            padding: 0.5rem 1rem;
            background: var(--bg-tertiary);
            color: var(--text-primary);
            border-radius: 5px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }

        .view-toggle a.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .view-toggle a:hover:not(.active) {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
        }

        .year-stats {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .year-stat {
            text-align: center;
            padding: 1rem 1.5rem;
            background: var(--card-bg);
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
        }

        .year-stat-value {
            font-size: 1.5rem;
            font-weight: bold;
        }

        .year-stat-label {
            font-size: 0.85rem;
            color: var(--text-secondary);
        }

        .year-stat.clean .year-stat-value {
            color: #28a745;
        }

        .year-stat.slip .year-stat-value {
            color: #856404;
        }

        .year-stat.failed .year-stat-value {
            color: #dc3545;
        }

        .year-stat.total .year-stat-value {
            color: #667eea;
        }

        .months-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 1.5rem;
        }

        .month-card {
            background: var(--card-bg);
            border-radius: 10px;
            padding: 1rem;
            box-shadow: 0 2px 10px var(--shadow-color);
        }

        .month-card-header {
            text-align: center;
            margin-bottom: 0.75rem;
        }

        .month-card-header a {
            color: var(--info-color);
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
        }

        .month-card-header a:hover {
            text-decoration: underline;
        }

        .mini-calendar {
            width: 100%;
        }

        .mini-weekdays {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            text-align: center;
            font-size: 0.7rem;
            color: var(--text-secondary);
            margin-bottom: 0.25rem;
        }

        .mini-days {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 2px;
        }

        .mini-day {
            aspect-ratio: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.75rem;
            border-radius: 4px;
            text-decoration: none;
            color: var(--text-primary);
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            transition: all 0.3s;
        }

        .mini-day.clean {
            background-color: rgba(40, 167, 69, 0.2);
            color: var(--success-color);
        }

        .mini-day.slip {
            background-color: rgba(255, 193, 7, 0.2);
            color: var(--warning-color);
        }

        .mini-day.failed {
            background-color: rgba(220, 53, 69, 0.2);
            color: var(--danger-color);
        }

        .mini-day.no-data {
            background-color: var(--bg-tertiary);
            color: var(--text-muted);
        }

        .mini-day.empty {
            background: transparent;
            border: none;
        }

        .mini-day:hover:not(.empty) {
            opacity: 0.8;
            transform: scale(1.1);
        }

        .legend {
            display: flex;
            justify-content: center;
            gap: 1.5rem;
            margin-top: 2rem;
            flex-wrap: wrap;
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.9rem;
        }

        .legend-color {
            width: 20px;
            height: 20px;
            border-radius: 4px;
        }

        .back-link {
            display: block;
            text-align: center;
            margin-top: 2rem;
            color: var(--info-color);
            text-decoration: none;
            font-weight: 600;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .calendar-year-container {
                padding: 15px;
            }

            .year-stats {
                gap: 1rem;
            }

            .year-stat {
                padding: 0.75rem 1rem;
            }

            .months-grid {
                grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
                gap: 1rem;
            }

            .mini-day {
                font-size: 0.65rem;
            }
        }

        @media (max-width: 480px) {
            .months-grid {
                grid-template-columns: 1fr;
            }

            .year-navigation {
                gap: 0.5rem;
            }

            .year-display {
                font-size: 1.25rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="calendar-year-container">
            <a href="/dashboard" class="back-link"
                style="display: inline-block; text-align: left; margin-bottom: 1rem;">← Back to Dashboard</a>

            <div class="calendar-year-header">
                <h1>📅 Year Calendar</h1>
                <p>View your progress by year</p>
            </div>

            <div class="view-toggle">
                <a href="/calendar">Month Calendar</a>
                <a href="/calendar/year/<%= year %>" class="active">Year Calendar</a>
            </div>

            <div class="year-navigation">
                <a href="/calendar/year/<%= year - 1 %>" class="year-nav-btn">← <%= year - 1 %></a>
                <span class="year-display">
                    <%= year %>
                </span>
                <a href="/calendar/year/<%= year + 1 %>" class="year-nav-btn">
                    <%= year + 1 %> →
                </a>
            </div>

            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
                <% } else { %>

                    <div class="year-stats">
                        <div class="year-stat clean">
                            <div class="year-stat-value">🟢 <%= stats.cleanDays %>
                            </div>
                            <div class="year-stat-label">Clean Days</div>
                        </div>
                        <div class="year-stat slip">
                            <div class="year-stat-value">🟡 <%= stats.slipDays %>
                            </div>
                            <div class="year-stat-label">Just a Little Bit Days</div>
                        </div>
                        <div class="year-stat failed">
                            <div class="year-stat-value">🔴 <%= stats.failedDays %>
                            </div>
                            <div class="year-stat-label">Totally Failed Days</div>
                        </div>
                        <div class="year-stat total">
                            <div class="year-stat-value">📊 <%= stats.totalDays %>
                            </div>
                            <div class="year-stat-label">Total Logged</div>
                        </div>
                    </div>

                    <div class="months-grid">
                        <% yearCalendar.forEach(monthData=> { %>
                            <div class="month-card">
                                <div class="month-card-header">
                                    <a href="/calendar/<%= year %>/<%= monthData.month %>">
                                        <%= monthData.monthName %>
                                    </a>
                                </div>
                                <div class="mini-calendar">
                                    <div class="mini-weekdays">
                                        <div>S</div>
                                        <div>M</div>
                                        <div>T</div>
                                        <div>W</div>
                                        <div>T</div>
                                        <div>F</div>
                                        <div>S</div>
                                    </div>
                                    <div class="mini-days">
                                        <% monthData.calendar.forEach(week=> { %>
                                            <% week.forEach(dayObj=> { %>
                                                <% if (dayObj) { %>
                                                    <a href="/entries/<%= dayObj.date %>"
                                                        class="mini-day <%= dayObj.status %>"
                                                        title="<%= dayObj.date %>: <%= dayObj.status === 'clean' ? 'Clean' : (dayObj.status === 'slip' ? 'Just a Little Bit' : (dayObj.status === 'failed' ? 'Totally Failed' : 'No entry')) %>">
                                                        <%= dayObj.day %>
                                                    </a>
                                                    <% } else { %>
                                                        <div class="mini-day empty"></div>
                                                        <% } %>
                                                            <% }); %>
                                                                <% }); %>
                                    </div>
                                </div>
                            </div>
                            <% }); %>
                    </div>

                    <div class="legend">
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #d4edda;"></div>
                            <span>🟢 Clean</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #fff3cd;"></div>
                            <span>🟡 Just a Little Bit</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #f8d7da;"></div>
                            <span>🔴 Totally Failed</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #f9f9f9;"></div>
                            <span>⚪ No Log</span>
                        </div>
                    </div>

                    <% } %>

                        <a href="/dashboard" class="back-link">← Back to Dashboard</a>
        </div>

        <%- include('partials/footer') %>
</body>

</html>