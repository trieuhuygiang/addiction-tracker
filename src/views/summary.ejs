<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thống kê | Cố Tinh Đạo Tông</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <script src="/js/theme-init.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=EB+Garamond:wght@400;500;600;700&family=Crimson+Text:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .summary-container {
            max-width: 1000px;
            margin: 2rem auto;
            padding: 0 20px;
        }

        .summary-header {
            margin-bottom: 2rem;
        }

        .summary-header h1 {
            color: var(--info-color);
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        .summary-header p {
            color: var(--text-secondary);
            font-size: 1rem;
        }

        .back-link {
            display: inline-block;
            color: var(--info-color);
            text-decoration: none;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .error-message {
            background: var(--card-bg);
            color: var(--danger-color);
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
            border: 1px solid color-mix(in srgb, var(--danger-color) 28%, transparent);
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--card-bg);
            padding: 1.5rem;
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
            text-align: center;
        }

        .stat-label {
            color: var(--text-secondary);
            font-size: 0.9rem;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .stat-value {
            font-size: 2.5rem;
            font-weight: bold;
            color: var(--info-color);
        }

        .stat-sublabel {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-top: 0.5rem;
        }

        .section {
            background: var(--card-bg);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
            margin-bottom: 2rem;
        }

        .section-title {
            font-size: 1.5rem;
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--info-color);
        }

        .table-container {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table th {
            background-color: var(--bg-tertiary);
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            color: var(--text-primary);
            border-bottom: 2px solid var(--border-color);
        }

        table td {
            padding: 0.75rem 1rem;
            border-bottom: 1px solid var(--border-color);
            color: var(--text-primary);
        }

        table tr:hover {
            background-color: var(--bg-tertiary);
        }

        .entry-clean {
            color: #155724;
            background-color: #d4edda;
            padding: 0.25rem 0.5rem;
            border-radius: 3px;
        }

        .entry-slip {
            color: #721c24;
            background-color: #f8d7da;
            padding: 0.25rem 0.5rem;
            border-radius: 3px;
        }

        .streak-item {
            background: var(--bg-tertiary);
            padding: 1rem;
            border-left: 4px solid #667eea;
            margin-bottom: 1rem;
            border-radius: 5px;
        }

        .streak-length {
            font-size: 1.5rem;
            font-weight: bold;
            color: #667eea;
            margin-right: 1rem;
            display: inline-block;
        }

        .streak-dates {
            color: #666;
            font-size: 0.9rem;
        }

        .streak-ongoing {
            background: linear-gradient(135deg, #d4edda 0%, #c3e6cb 100%);
            border-left-color: #28a745;
        }

        .empty-message {
            text-align: center;
            padding: 2rem;
            color: #999;
        }

        @media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .stat-value {
                font-size: 2rem;
            }

            .section {
                padding: 1rem;
            }

            .section-title {
                font-size: 1.2rem;
            }

            table {
                font-size: 0.9rem;
            }

            table th,
            table td {
                padding: 0.5rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="summary-container">
            <a href="/dashboard" class="back-link">← Về tổng quan</a>

            <div class="summary-header">
                <h1>📊 Thống kê tu luyện</h1>
                <p>Tổng quan tiến độ tu luyện của bạn</p>
            </div>

            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
                <% } %>

                    <!-- Main Statistics -->
                    <div class="stats-grid">
                        <div class="stat-card">
                            <div class="stat-label">Ngày tu luyện</div>
                            <div class="stat-value">🔥 <%= stats.totalRewireDays %>
                            </div>
                            <div class="stat-sublabel">ngày theo bộ đếm</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Phiên tu dài nhất</div>
                            <div class="stat-value">⏱️ <%= stats.longestRewireSessionDays %>d <%=
                                        stats.longestRewireSessionHours %>h
                            </div>
                            <div class="stat-sublabel">phiên bộ đếm dài nhất</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Ngày thanh tịnh</div>
                            <div class="stat-value">🟢 <%= stats.cleanDays %>
                            </div>
                            <div class="stat-sublabel">ngày thanh tịnh</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Ngày sơ suất</div>
                            <div class="stat-value">🔴 <%= stats.slipDays %>
                            </div>
                            <div class="stat-sublabel">ngày</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Ngày thất thủ</div>
                            <div class="stat-value">💀 <%= stats.totallyFailedDays %>
                            </div>
                            <div class="stat-sublabel">ngày</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Dấu hiệu hồi phục (sáng)</div>
                            <div class="stat-value">🌳 <%= stats.morningWoodCount %>
                            </div>
                            <div class="stat-sublabel">chỉ dấu hồi phục</div>
                        </div>

                        <div class="stat-card">
                            <div class="stat-label">Tỷ lệ thành công</div>
                            <div class="stat-value">📈 <%= stats.successRate %>%</div>
                            <div class="stat-sublabel">ngày thanh tịnh</div>
                        </div>
                    </div>

                    <% if (stats.totalRewireDays> 0 || stats.totalDays > 0) { %>
                        <!-- Streak History -->
                        <div class="section">
                            <div class="section-title">📋 Lịch sử chuỗi</div>
                            <% if (streaksData.length> 0) { %>
                                <% streaksData.forEach((streak, index)=> { %>
                                    <div class="streak-item <%= streak.ongoing ? 'streak-ongoing' : '' %>">
                                        <span class="streak-length">
                                            <%= streak.length %> ngày
                                        </span>
                                        <div class="streak-dates">
                                            <%= streak.startDate %> đến <%= streak.endDate %>
                                                    <% if (streak.ongoing) { %>
                                                        <strong>(Đang tiếp diễn 🔥)</strong>
                                                        <% } %>
                                        </div>
                                    </div>
                                    <% }); %>
                                        <% } else { %>
                                            <div class="empty-message">Chưa có chuỗi nào được ghi nhận</div>
                                            <% } %>
                        </div>

                        <!-- Recent Entries -->
                        <div class="section">
                            <div class="section-title">📝 Ghi chép gần đây</div>
                            <% if (stats.recentEntries.length> 0) { %>
                                <div class="table-container">
                                    <table>
                                        <thead>
                                            <tr>
                                                <th>Ngày</th>
                                                <th>Trạng thái</th>
                                                <th>Ghi chú</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% stats.recentEntries.forEach(entry=> { %>
                                                <tr>
                                                    <td>
                                                        <%= entry.dateFormatted %>
                                                    </td>
                                                    <td>
                                                        <span
                                                            class="<%= entry.had_leakage ? 'entry-slip' : 'entry-clean' %>">
                                                            <% const statusMap={ Clean: 'Thanh tịnh'
                                                                , 'Just a Little Bit' : 'Sơ suất' , 'Totally Failed'
                                                                : 'Thất thủ' }; %>
                                                                <%= statusMap[entry.statusText] || entry.statusText %>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <%= entry.note ? entry.note : '-' %>
                                                    </td>
                                                </tr>
                                                <% }); %>
                                        </tbody>
                                    </table>
                                </div>
                                <% } else { %>
                                    <div class="empty-message">Chưa có ghi chép nào</div>
                                    <% } %>
                        </div>
                        <% } else { %>
                            <div class="section">
                                <div class="empty-message">
                                    <p>Bạn chưa ghi chép lần nào. Hãy bắt đầu hành trình tu luyện bằng cách <a
                                            href="/entries"
                                            style="color: var(--info-color); text-decoration: underline;">tạo ghi chép
                                            đầu tiên</a>!</p>
                                </div>
                            </div>
                            <% } %>
        </div>

        <%- include('partials/footer') %>
</body>

</html>