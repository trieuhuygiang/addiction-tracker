services:
  - type: web
    name: addiction-tracker
    runtime: node
    buildCommand: npm install && npm run setup
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: USE_HTTPS
        value: true
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
