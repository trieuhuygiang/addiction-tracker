<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title || 'Cultivation Center' %>
    </title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <script src="/js/theme-init.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=EB+Garamond:wght@400;500;600;700&family=Crimson+Text:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .calendar-container {
            max-width: 900px;
            margin: 2rem auto;
            padding: 0 20px;
        }

        .calendar-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .calendar-header h1 {
            color: #667eea;
            font-size: 2rem;
            margin-bottom: 0.5rem;
        }

        .calendar-header p {
            color: var(--text-secondary);
            font-size: 1rem;
        }

        .month-navigation {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .nav-btn {
            padding: 0.5rem 1rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: 600;
            text-decoration: none;
            transition: transform 0.3s;
        }

        .nav-btn:hover {
            transform: translateY(-2px);
        }

        .month-display {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--text-primary);
            min-width: 200px;
            text-align: center;
        }

        .calendar-table {
            background: var(--card-bg);
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 10px var(--shadow-color);
            margin-bottom: 2rem;
        }

        .calendar-weekdays {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: 600;
        }

        .weekday {
            padding: 1rem;
            text-align: center;
            border-right: 1px solid rgba(255, 255, 255, 0.2);
        }

        .weekday:last-child {
            border-right: none;
        }

        .calendar-days {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
        }

        .calendar-day {
            aspect-ratio: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            border: 1px solid var(--border-color);
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            font-weight: 600;
            background: var(--bg-secondary);
            color: var(--text-primary);
        }

        .calendar-day:hover {
            transform: scale(1.05);
        }

        .calendar-day.clean {
            background-color: rgba(40, 167, 69, 0.2);
            color: var(--success-color);
        }

        .calendar-day.clean:hover {
            background-color: rgba(40, 167, 69, 0.3);
        }

        .calendar-day.slip {
            background-color: rgba(255, 193, 7, 0.2);
            color: var(--warning-color);
        }

        .calendar-day.slip:hover {
            background-color: rgba(255, 193, 7, 0.3);
        }

        .calendar-day.failed {
            background-color: rgba(220, 53, 69, 0.2);
            color: var(--danger-color);
        }

        .calendar-day.failed:hover {
            background-color: rgba(220, 53, 69, 0.3);
        }

        .calendar-day.no-data {
            background-color: var(--bg-tertiary);
            color: var(--text-muted);
        }

        .calendar-day.no-data:hover {
            background-color: var(--bg-secondary);
        }

        .calendar-day.other-month {
            background-color: var(--bg-primary);
            color: var(--text-muted);
            opacity: 0.5;
            cursor: default;
        }

        .calendar-day.other-month:hover {
            transform: none;
            background-color: var(--bg-primary);
        }

        .day-number {
            font-size: 1.1rem;
        }

        .day-indicator {
            font-size: 0.8rem;
            margin-top: 0.2rem;
        }

        .legend {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .legend-color {
            width: 30px;
            height: 30px;
            border-radius: 5px;
            border: 1px solid var(--border-color);
        }

        .back-link {
            display: inline-block;
            color: var(--info-color);
            text-decoration: none;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .error-message {
            background-color: #f8d7da;
            color: #721c24;
            padding: 1rem;
            border-radius: 5px;
            margin-bottom: 1.5rem;
            border: 1px solid #f5c6cb;
        }

        /* Enhanced Mobile Responsive Styles */
        @media (max-width: 768px) {
            .calendar-container {
                padding: 0 10px;
                margin: 1rem auto;
            }

            .calendar-header h1 {
                font-size: 1.5rem;
            }

            .calendar-header p {
                font-size: 0.9rem;
            }

            .month-navigation {
                gap: 0.5rem;
                flex-wrap: wrap;
            }

            .nav-btn {
                padding: 0.5rem 0.75rem;
                font-size: 0.85rem;
            }

            .month-display {
                font-size: 1.1rem;
                min-width: auto;
                order: -1;
                width: 100%;
                margin-bottom: 0.5rem;
            }

            .weekday {
                padding: 0.5rem 0.25rem;
                font-size: 0.75rem;
            }

            .calendar-day {
                font-size: 0.8rem;
                aspect-ratio: auto;
                min-height: 45px;
                padding: 0.25rem;
            }

            .day-number {
                font-size: 0.85rem;
            }

            .day-indicator {
                font-size: 0.7rem;
            }

            .legend {
                gap: 0.75rem;
                font-size: 0.85rem;
            }

            .legend-color {
                width: 20px;
                height: 20px;
            }
        }

        @media (max-width: 480px) {
            .calendar-header h1 {
                font-size: 1.25rem;
            }

            .weekday {
                padding: 0.4rem 0.1rem;
                font-size: 0.65rem;
            }

            .calendar-day {
                min-height: 38px;
                font-size: 0.75rem;
            }

            .day-number {
                font-size: 0.75rem;
            }

            .day-indicator {
                font-size: 0.6rem;
            }

            .legend {
                flex-direction: column;
                align-items: center;
                gap: 0.5rem;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="calendar-container">
            <a href="/dashboard" class="back-link">← Back to Dashboard</a>

            <div class="calendar-header">
                <h1>📅 Month Calendar</h1>
                <p>View your progress by month</p>
            </div>

            <div class="view-toggle" style="display: flex; justify-content: center; gap: 0.5rem; margin-bottom: 2rem;">
                <a href="/calendar/<%= year %>/<%= month %>"
                    style="padding: 0.5rem 1rem; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border-radius: 5px; text-decoration: none; font-weight: 600;">Month
                    Calendar</a>
                <a href="/calendar/year/<%= year %>"
                    style="padding: 0.5rem 1rem; background: var(--bg-tertiary); color: var(--text-primary); border-radius: 5px; text-decoration: none; font-weight: 600; transition: all 0.3s;">Year
                    Calendar</a>
            </div>

            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
                <% } %>

                    <div class="month-navigation">
                        <a href="/calendar/<%= month === 1 ? year - 1 : year %>/<%= month === 1 ? 12 : month - 1 %>"
                            class="nav-btn">
                            ← Previous
                        </a>
                        <div class="month-display">
                            <%= monthName %>
                                <%= year %>
                        </div>
                        <a href="/calendar/<%= month === 12 ? year + 1 : year %>/<%= month === 12 ? 1 : month + 1 %>"
                            class="nav-btn">
                            Next →
                        </a>
                    </div>

                    <div class="legend">
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #d4edda;"></div>
                            <span>✅ Clean</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #fff3cd;"></div>
                            <span>⚠️ A Little Bit</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #f8d7da;"></div>
                            <span>💀 Totally Failed</span>
                        </div>
                        <div class="legend-item">
                            <div class="legend-color" style="background-color: #f9f9f9;"></div>
                            <span>⚪ No Log</span>
                        </div>
                    </div>

                    <% if (calendar) { %>
                        <div class="calendar-table">
                            <div class="calendar-weekdays">
                                <div class="weekday">Sun</div>
                                <div class="weekday">Mon</div>
                                <div class="weekday">Tue</div>
                                <div class="weekday">Wed</div>
                                <div class="weekday">Thu</div>
                                <div class="weekday">Fri</div>
                                <div class="weekday">Sat</div>
                            </div>

                            <div class="calendar-days">
                                <% calendar.forEach(week=> { %>
                                    <% week.forEach(dayObj=> { %>
                                        <% if (dayObj) { %>
                                            <a href="/entries/<%= dayObj.date %>"
                                                class="calendar-day <%= dayObj.status %>"
                                                title="<%= dayObj.status === 'clean' ? 'Clean' : dayObj.status === 'slip' ? 'A Little Bit' : dayObj.status === 'failed' ? 'Totally Failed' : 'No entry' %><%= dayObj.entry && dayObj.entry.has_morning_wood ? ' + Tree of Life 🌳' : '' %>">
                                                <div class="day-number">
                                                    <%= dayObj.day %>
                                                </div>
                                                <div class="day-indicator">
                                                    <% if (dayObj.status==='clean' ) { %>
                                                        ✅
                                                        <% } else if (dayObj.status==='slip' ) { %>
                                                            ⚠️
                                                            <% } else if (dayObj.status==='failed' ) { %>
                                                                💀
                                                                <% } else { %>
                                                                    ⚪
                                                                    <% } %>
                                                </div>
                                                <% if (dayObj.entry && dayObj.entry.has_morning_wood) { %>
                                                    <div style="font-size: 0.9rem; margin-top: 2px;">🌳</div>
                                                    <% } %>
                                            </a>
                                            <% } else { %>
                                                <div class="calendar-day other-month"></div>
                                                <% } %>
                                                    <% }); %>
                                                        <% }); %>
                            </div>
                        </div>
                        <% } %>

                            <div style="text-align: center; color: #666; margin-top: 2rem;">
                                <p>Click any day to view or edit the entry</p>
                            </div>
        </div>

        <%- include('partials/footer') %>
</body>

</html>