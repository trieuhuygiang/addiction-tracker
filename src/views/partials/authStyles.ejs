<style>
    .auth-container {
        max-width: 400px;
        margin: 3rem auto;
        padding: 2rem;
        background: rgba(20, 20, 31, 0.85);
        border-radius: 10px;
        box-shadow: 0 8px 32px rgba(139, 0, 0, 0.4);
        backdrop-filter: blur(10px);
        border: 2px solid rgba(61, 52, 40, 0.6);
    }

    .auth-container h1 {
        text-align: center;
        color: #d4c8b8;
        margin-bottom: 2rem;
        text-shadow: 2px 2px 8px rgba(139, 0, 0, 0.6);
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-group label {
        display: block;
        margin-bottom: 0.5rem;
        color: #a39888;
        font-weight: 600;
    }

    .form-group input {
        width: 100%;
        padding: 0.75rem;
        border: 1px solid #3d3428;
        border-radius: 5px;
        font-size: 1rem;
        transition: border-color 0.3s;
        background: rgba(31, 31, 46, 0.9);
        color: #d4c8b8;
    }

    .form-group input:focus {
        outline: none;
        border-color: #8b0000;
        box-shadow: 0 0 5px rgba(139, 0, 0, 0.5);
    }

    .error {
        background-color: rgba(139, 0, 0, 0.3);
        color: #ff6b6b;
        padding: 0.75rem;
        border-radius: 5px;
        margin-bottom: 1.5rem;
        border: 1px solid #8b0000;
    }

    .submit-btn {
        width: 100%;
        padding: 0.75rem;
        background: linear-gradient(135deg, rgba(31, 31, 46, 0.95) 0%, rgba(74, 22, 22, 0.95) 100%);
        color: #d4c8b8;
        border: 2px solid rgba(61, 52, 40, 0.6);
        border-radius: 5px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: transform 0.3s, box-shadow 0.3s;
    }

    .submit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(139, 0, 0, 0.6);
    }

    .auth-link {
        text-align: center;
        margin-top: 1.5rem;
        color: #a39888;
    }

    .auth-link a {
        color: #d4a005;
        text-decoration: none;
        font-weight: 600;
    }

    .auth-link a:hover {
        text-decoration: underline;
        color: #f5c542;
    }
</style>