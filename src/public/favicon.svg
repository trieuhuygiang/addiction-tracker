<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <style>
      @keyframes shimmer {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.7; }
      }
      .sparkle { animation: shimmer 2s infinite; }
    </style>
  </defs>
  <circle cx="50" cy="50" r="48" fill="none" stroke="#9370DB" stroke-width="2" opacity="0.3"/>
  <text y=".9em" font-size="90" class="sparkle">✨</text>
</svg>
