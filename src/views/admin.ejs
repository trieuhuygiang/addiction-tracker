<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản trị | Cố Tinh Đạo Tông</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=EB+Garamond:wght@400;500;600;700&family=Crimson+Text:wght@400;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .admin-container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 2rem;
        }

        .admin-header {
            background: linear-gradient(135deg, var(--gradient-start) 0%, var(--gradient-end) 100%);
            color: white;
            padding: 2rem;
            border-radius: 10px;
            margin-bottom: 2rem;
        }

        .admin-header h1 {
            margin: 0;
            font-size: 2rem;
        }

        .admin-header p {
            margin: 0.5rem 0 0;
            opacity: 0.9;
        }

        .admin-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--card-bg);
            padding: 1.5rem;
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
            text-align: center;
        }

        .stat-card h3 {
            color: var(--info-color);
            font-size: 2rem;
            margin: 0;
        }

        .stat-card p {
            color: var(--text-secondary);
            margin: 0.5rem 0 0;
        }

        .admin-section {
            background: var(--card-bg);
            border-radius: 10px;
            box-shadow: 0 2px 10px var(--shadow-color);
            margin-bottom: 2rem;
            overflow: hidden;
        }

        .section-header {
            background: var(--bg-tertiary);
            padding: 1rem 1.5rem;
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .section-header h2 {
            margin: 0;
            color: var(--text-primary);
            font-size: 1.25rem;
        }

        .users-table {
            width: 100%;
            border-collapse: collapse;
        }

        .users-table th,
        .users-table td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid var(--border-color);
            color: var(--text-primary);
        }

        .users-table th {
            background: var(--bg-tertiary);
            font-weight: 600;
            color: var(--text-primary);
        }

        .users-table tbody tr:hover {
            background: var(--bg-tertiary);
        }

        .badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .badge-admin {
            background: var(--info-color);
            color: white;
        }

        .badge-user {
            background: var(--bg-tertiary);
            color: var(--text-secondary);
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 0.85rem;
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            transition: all 0.3s;
        }

        .btn-primary {
            background: var(--info-color);
            color: white;
        }

        .btn-primary:hover {
            background: color-mix(in srgb, var(--info-color) 82%, black);
        }

        .btn-success {
            background: var(--success-color);
            color: white;
        }

        .btn-success:hover {
            background: color-mix(in srgb, var(--success-color) 82%, black);
        }

        .btn-warning {
            background: var(--warning-color);
            color: white;
        }

        .btn-warning:hover {
            background: color-mix(in srgb, var(--warning-color) 82%, black);
        }

        .btn-danger {
            background: var(--danger-color);
            color: white;
        }

        .btn-danger:hover {
            background: color-mix(in srgb, var(--danger-color) 82%, black);
        }

        .btn-sm {
            padding: 0.35rem 0.75rem;
            font-size: 0.8rem;
        }

        .alert {
            padding: 1rem 1.5rem;
            border-radius: 5px;
            margin-bottom: 1rem;
        }

        .alert-success {
            background: var(--card-bg);
            color: var(--success-color);
            border: 1px solid color-mix(in srgb, var(--success-color) 28%, transparent);
        }

        .alert-error {
            background: var(--card-bg);
            color: var(--danger-color);
            border: 1px solid color-mix(in srgb, var(--danger-color) 28%, transparent);
        }

        /* Modal styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
        }

        .modal.active {
            display: flex;
        }

        .modal-content {
            background: var(--card-bg);
            padding: 2rem;
            border-radius: 10px;
            width: 100%;
            max-width: 500px;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .modal-header h3 {
            margin: 0;
            color: var(--text-primary);
        }

        .modal-close {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-secondary);
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid var(--border-color);
            border-radius: 5px;
            font-size: 1rem;
        }

        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--info-color);
        }

        .form-check {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .form-check input {
            width: auto;
        }

        .no-users {
            text-align: center;
            padding: 3rem;
            color: var(--text-secondary);
        }

        @media (max-width: 768px) {
            .users-table {
                display: block;
                overflow-x: auto;
            }

            .action-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="admin-container">
            <div class="admin-header">
                <h1>👑 Bảng quản trị</h1>
                <p>Quản lý đệ tử và theo dõi hoạt động hệ thống</p>
            </div>

            <% if (success) { %>
                <div class="alert alert-success">
                    <%= success %>
                </div>
                <% } %>

                    <% if (error) { %>
                        <div class="alert alert-error">
                            <%= error %>
                        </div>
                        <% } %>

                            <div class="admin-stats">
                                <div class="stat-card">
                                    <h3>
                                        <%= users.length %>
                                    </h3>
                                    <p>Tổng số đệ tử</p>
                                </div>
                                <div class="stat-card">
                                    <h3>
                                        <%= users.filter(u=> u.is_admin).length %>
                                    </h3>
                                    <p>Quản trị</p>
                                </div>
                                <div class="stat-card">
                                    <h3>
                                        <%= users.reduce((sum, u)=> sum + u.entry_count, 0) %>
                                    </h3>
                                    <p>Tổng ghi chép</p>
                                </div>
                            </div>

                            <div class="admin-section">
                                <div class="section-header">
                                    <h2>👥 Quản lý đệ tử</h2>
                                    <div style="display: flex; gap: 0.5rem; flex-wrap: wrap;">
                                        <form method="POST" action="/admin/auto-track" style="display:inline;">
                                            <button type="submit" class="btn btn-primary"
                                                onclick="return confirm('Chạy auto-track ngay? Hệ thống sẽ đánh dấu tất cả người dùng chưa ghi chép hôm nay là Thanh tịnh.')">🤖
                                                Tự điểm danh (Thanh tịnh)</button>
                                        </form>
                                        <button class="btn btn-success" onclick="openModal('createModal')">➕ Thêm
                                            đệ tử</button>
                                    </div>
                                </div>

                                <% if (users.length> 0) { %>
                                    <table class="users-table">
                                        <thead>
                                            <tr>
                                                <th>ID</th>
                                                <th>Email</th>
                                                <th>Tên đăng nhập</th>
                                                <th>Vai trò</th>
                                                <th>Ghi chép</th>
                                                <th>Ngày tạo</th>
                                                <th>Thao tác</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% users.forEach(user=> { %>
                                                <tr>
                                                    <td>
                                                        <%= user.id %>
                                                    </td>
                                                    <td>
                                                        <%= user.email %>
                                                    </td>
                                                    <td>
                                                        <%= user.username || '-' %>
                                                    </td>
                                                    <td>
                                                        <% if (user.is_admin) { %>
                                                            <span class="badge badge-admin">👑 Quản trị</span>
                                                            <% } else { %>
                                                                <span class="badge badge-user">Đệ tử</span>
                                                                <% } %>
                                                    </td>
                                                    <td>
                                                        <%= user.entry_count %>
                                                    </td>
                                                    <td>
                                                        <%= new Date(user.created_at).toLocaleDateString('vi-VN') %>
                                                    </td>
                                                    <td>
                                                        <div class="action-buttons">
                                                            <button class="btn btn-primary btn-sm"
                                                                onclick="openEditModal('<%= user.id %>', '<%= user.username || '' %>', <%= user.is_admin %>)">
                                                                ✏️ Sửa
                                                            </button>
                                                            <% if (user.id !=userId) { %>
                                                                <form method="POST"
                                                                    action="/admin/users/<%= user.id %>/toggle-admin"
                                                                    style="display:inline;">
                                                                    <button type="submit"
                                                                        class="btn btn-warning btn-sm">
                                                                        <% if (user.is_admin) { %>
                                                                            👤 Gỡ quyền quản trị
                                                                            <% } else { %>
                                                                                👑 Phong quản trị
                                                                                <% } %>
                                                                    </button>
                                                                </form>
                                                                <form method="POST"
                                                                    action="/admin/users/<%= user.id %>/delete"
                                                                    style="display:inline;"
                                                                    onsubmit="return confirm('Bạn chắc chắn muốn xoá người dùng này? Hành động này không thể hoàn tác.')">
                                                                    <button type="submit"
                                                                        class="btn btn-danger btn-sm">🗑️
                                                                        Xoá</button>
                                                                </form>
                                                                <% } %>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% }); %>
                                        </tbody>
                                    </table>
                                    <% } else { %>
                                        <div class="no-users">
                                            <p>Chưa có người dùng.</p>
                                        </div>
                                        <% } %>
                            </div>
        </div>

        <!-- Create User Modal -->
        <div id="createModal" class="modal">
            <div class="modal-content">
                <div class="modal-header">
                    <h3>➕ Tạo đệ tử mới</h3>
                    <button class="modal-close" onclick="closeModal('createModal')">&times;</button>
                </div>
                <form method="POST" action="/admin/users/create">
                    <div class="form-group">
                        <label for="email">Email *</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="username">Tên đăng nhập</label>
                        <input type="text" id="username" name="username">
                    </div>
                    <div class="form-group">
                        <label for="password">Mật khẩu *</label>
                        <input type="password" id="password" name="password" required minlength="6">
                    </div>
                    <div class="form-group">
                        <label for="confirmPassword">Xác nhận mật khẩu *</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" required minlength="6">
                    </div>
                    <div class="form-group">
                        <div class="form-check">
                            <input type="checkbox" id="isAdmin" name="isAdmin">
                            <label for="isAdmin">Phong người dùng này làm quản trị</label>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-success" style="width:100%;">Tạo người dùng</button>
                </form>
            </div>
        </div>

        <!-- Edit User Modal -->
        <div id="editModal" class="modal">
            <div class="modal-content">
                <div class="modal-header">
                    <h3>✏️ Sửa người dùng</h3>
                    <button class="modal-close" onclick="closeModal('editModal')">&times;</button>
                </div>
                <form method="POST" action="/admin/users/update">
                    <input type="hidden" id="editUserId" name="userId">
                    <div class="form-group">
                        <label for="editUsername">Tên đăng nhập</label>
                        <input type="text" id="editUsername" name="username">
                    </div>
                    <div class="form-group">
                        <label for="editPassword">Mật khẩu mới (để trống nếu giữ nguyên)</label>
                        <input type="password" id="editPassword" name="password">
                    </div>
                    <div class="form-group">
                        <div class="form-check">
                            <input type="checkbox" id="editIsAdmin" name="isAdmin">
                            <label for="editIsAdmin">Quyền quản trị</label>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary" style="width:100%;">Cập nhật</button>
                </form>
            </div>
        </div>

        <%- include('partials/footer') %>

            <script>
                function openModal(modalId) {
                    document.getElementById(modalId).classList.add('active');
                }

                function closeModal(modalId) {
                    document.getElementById(modalId).classList.remove('active');
                }

                function openEditModal(id, username, isAdmin) {
                    document.getElementById('editUserId').value = id;
                    document.getElementById('editUsername').value = username;
                    document.getElementById('editIsAdmin').checked = isAdmin;
                    openModal('editModal');
                }

                // Close modal when clicking outside
                document.querySelectorAll('.modal').forEach(modal => {
                    modal.addEventListener('click', (e) => {
                        if (e.target === modal) {
                            modal.classList.remove('active');
                        }
                    });
                });
            </script>
</body>

</html>